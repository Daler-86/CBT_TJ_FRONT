import { SplitByDashPipe } from './split-by-dash.pipe';

describe('SplitByDashPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitByDashPipe();
    expect(pipe).toBeTruthy();
  });
});
